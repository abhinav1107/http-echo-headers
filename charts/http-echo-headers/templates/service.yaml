apiVersion: v1
kind: Service
metadata:
  name: {{ include "echo-headers.fullname" . }}
  labels:
    {{- include "echo-headers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "echo-headers.selectorLabels" . | nindent 4 }}
